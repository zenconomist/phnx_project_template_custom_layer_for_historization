version: '3.8'

services:
  db:
    image: default_docker
    # build:
      # context: .
      # dockerfile: Dockerfile
    ports:
      - "5459:5432"
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
      # - POSTGRES_PORT=5432

# volumes:
#   postgres_data:
